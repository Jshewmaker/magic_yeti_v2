name: user_repository
description: Dart package which manages the user domain.
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  authentication_client:
    path: ../authentication_client/authentication_client
  equatable: ^2.0.5
  firebase_database_repository:
    path: ../firebase_database_repository
  rxdart: ^0.28.0

dev_dependencies:
  mocktail: ^1.0.3
  test: ^1.25.2
  very_good_analysis: ^6.0.0
