name: firebase_database_repository
description: Firebase database package
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  mocktail: ^1.0.0
  test: ^1.19.2
  very_good_analysis: ^5.1.0
dependencies:
  cloud_firestore: ^4.15.3
  firebase_database: ^10.4.4
